# 接入携程 eBooking 点评（浏览器书签方式）

> 目标：携程商家后台来新点评时，**一键把点评搬进智能体**，自动分级并生成回复；回复由人工确认后粘回携程。
> 不需要服务器、不需要携程开放接口权限、不需要填任何密钥。

---

## 一、为什么用这种方式

| 方案 | 是否需要申请 | 是否需要服务器 | 说明 |
| --- | --- | --- | --- |
| 携程开放接口 | ✅ 需签约 + 门店授权 + 点评数据单独申请 | ✅ 需要（密钥不能放前端） | 最稳，但要走商务流程 |
| **浏览器书签（本方案）** | ❌ 不需要 | ❌ 不需要 | 用你已登录的后台页面读取点评，把内容搬进自己的工具 |
| 手工复制粘贴 | ❌ 不需要 | ❌ 不需要 | 最原始，每条点评都要手动复制 |

书签方式适合：**先跑起来、不折腾申请流程**。等以后拿到官方接口权限，再换成接口对接即可（工具的导入接口是通用的，换数据源不影响使用）。

---

## 二、安装（约 1 分钟）

1. 打开 **https://1339171078.github.io/hotel-review-agent/bookmarklet.html**
2. 把页面里蓝色的 **「📥 抓取携程点评」** 按钮**用鼠标拖动到浏览器书签栏**
   - 看不到书签栏？按 `⌘ + Shift + B`（Windows：`Ctrl + Shift + B`）
   - ⚠️ 是**拖动**，不是点击
3. 完成。以后它就一直在书签栏里。

> 打开的是线上页面，需要能访问 github.io（国内可能较慢，多刷几次）。
> 也可以本地部署后从内网地址打开安装页，同样可用。

---

## 三、使用（首次 3 步，之后 2 步）

**首次：**

1. 打开携程 eBooking 商家后台 → **点评管理 / 点评回复** 页面
2. 点一下书签栏里的「📥 抓取携程点评」
3. 右下角会提示"请点击页面上的一条点评卡片" → **你就点一条点评**（工具据此记住页面结构）
   → 面板显示"识别到 N 条点评" → 点 **「导入到智能体」**

**之后每次：**

1. 翻到点评页 → 点书签 → 「导入到智能体」
2. 智能体自动打开，点评已进入批量列表，直接点「⚡ 批量生成」

> 抓取的是**当前页**的点评。携程分页的话，翻一页点一次即可；已导入过的点评会自动去重，不会重复。

---

## 四、抓取到的内容

每条点评会尽量解析出：

| 字段 | 说明 |
| --- | --- |
| 评分 | 1~5 分（识别"5.0分"、星级图标等） |
| 内容 | 点评正文（自动剔除星级符号、日期、房型等元信息，也会截掉"商家回复"部分） |
| 日期 | 如 2026-03-12 |
| 昵称 | 顾客昵称 |
| 房型 | 如 行政大床房 |
| 来源 | 自动标记为「携程 eBooking」，在批量列表里可以看到 |

导入后工具会**自动分级**（好评/中评/差评）、识别五维度、标记严重问题（虫害、隐私、财物等）。

---

## 五、常见问题

| 现象 | 处理 |
| --- | --- |
| 没识别到点评 | 点面板里的 **「点选方式」**，再点一条点评卡片 |
| 识别到了无关内容 | 同样用「点选方式」重新指定一条**真正的点评卡片** |
| 没跳到智能体 | 点评已复制到剪贴板 → 到智能体「批量处理 → 📥 从携程导入」粘贴 JSON |
| 想要 JSON 手工处理 | 面板点「复制 JSON」，可自行保存或粘到别处 |
| 携程改版后失效 | 重新用「点选方式」指定一次，选择器会重新记忆 |
| 换电脑 / 换浏览器 | 需要重新拖一次书签、重新点选一次 |
| 书签点了没反应 | 确认当前在 `ebooking.ctrip.com` 页面；部分浏览器需允许该站点运行书签脚本 |

---

## 六、只想手工导入也可以

智能体「批量处理」卡片里有 **📥 从携程导入** 折叠区：

- 把书签面板「复制 JSON」的内容粘进去 → 点「导入这批点评」
- 或者手写更简单的格式也行，每行一条：

```
5|前台热情，房间干净，位置方便，下次还来
3|空调不太制冷，毛巾有点旧
1|房间有蟑螂，床单有污渍
```

---

## 七、先在本机试一遍（推荐）

不想直接上携程试，可以先拿模拟页面练手：

**https://1339171078.github.io/hotel-review-agent/demo/ebooking-mock.html**

里面是 6 条虚构点评（含星级、日期、房型、商家回复等），操作方式和真实后台一样。
试通之后再上携程，心里有底。

---

## 八、合规与风险提示

- 本方案通过**你已登录的浏览器页面**读取点评内容，属于**辅助录入**，不破解、不绕过携程任何鉴权。
- 使用前请确认符合**携程平台服务条款**及贵店的内控要求；若平台明确禁止页面辅助工具，请改用官方开放接口。
- **不做自动发布**：点评只被"搬进来"，回复仍由人工确认后手工粘回后台，避免模板腔或误判直接对外可见。
- **数据不出本机**：抓取、分析、生成全过程在本地浏览器完成，不经过任何第三方服务器；书签脚本源码公开可查（`bookmarklet.js`）。
- 书签里的选择器会保存在 `ebooking.ctrip.com` 这个域下（浏览器本地），不会上传。

---

## 九、以后想换成官方接口

工具侧的导入通道是**数据源无关**的：任何数据源只要能产出下面这个结构，就能接入。

```json
{
  "src": "携程 eBooking",
  "items": [
    { "rating": 3, "text": "点评内容", "guest": "昵称", "date": "2026-03-12", "room": "行政大床房" }
  ]
}
```

所以将来你申请到开放接口后，只需要让接口把数据按这个格式吐出来（可放在你自己的服务端，密钥不下发到前端），
前端一行都不用改。届时把接口文档给我，我来写对接。
